1. each value must be unique
2. they cannot be NULL
3. they can oly be one primary key in a table

CREATE TABLE quotes(
        id INT UNSIGNED NOT NULL AUTO-INCREMENrt
        author_first VARCHAR(50)
        author_last VARCHAR(100) NOT NULL,
        content TEXT NOT NULLPrimary KEY(id)); [this indicates primary key]

------------------------------getting info about existing table-----------------------------------

DESCRIBE table_name;
    - explore the structure of a single table 
        . shows the same thing as the structure table

SHOW CREATE TABLE table_nane; 
    - display original command used to create table



